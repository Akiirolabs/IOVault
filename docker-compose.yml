services:
  io-vault:
    build: .
    ports:
      - "${API_PORT:-8787}:8787"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      API_HOST: 0.0.0.0
      API_PORT: 8787
    restart: unless-stopped
