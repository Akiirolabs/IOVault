interface:
  display_name: "Work Review"
  short_description: "Route work through the DPL lifecycle"
  default_prompt: "Classify this IO Vault work within the DPL, IMP, FTR, SEC, SYS, and DBG lifecycle, then synchronize its authoritative records."
