interface:
  display_name: "Work Review"
  short_description: "Route evidence into IO Vault work codes"
  default_prompt: "Classify this IO Vault review or audit, preserve its source code, create the correct work record, and synchronize the ledger."
