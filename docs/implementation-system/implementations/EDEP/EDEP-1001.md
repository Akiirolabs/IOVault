# EDEP-1001 — Mentor Agent External Dependencies

**Owner:** [IMP-1004 — Mentor Agent](../IMP-1004-learning-mentor.md) · **State:** Open

EDEP-1001 records the services, credentials, consent, and hosted infrastructure required to complete the Mentor Agent beyond its working internal runtime.

| Work code | External dependency | Enables | Status | Evidence / next |
|---|---|---|---|---|
| `EDEP-1001.1` | Production `OPENAI_API_KEY` and configured `OPENAI_AGENT_MODEL` | Mentor reasoning, tool selection, transcription, and spoken responses | Open | Local key and `gpt-5.6-terra` live response verified 2026-07-31; production configuration remains |
| `EDEP-1001.2` | Production `CONNECTOR_ENCRYPTION_KEY` containing at least 32 random characters | Encryption of Google access and refresh tokens at rest | Open | Production secret required |
| `EDEP-1001.3` | Continuously available Express runtime with writable SQLite storage | Durable background tasks, recovery, schedules, and connector actions while the browser is closed | Open | Hetzner VPS selected; persistent service, storage, backup, and recovery verification remain |
| `EDEP-1001.4` | HTTPS application origin and an exact registered Google redirect URI | Secure completion of Google OAuth | Open | `app.akiiro.com` Cloudflare routing selected; DNS, strict TLS, and callback deployment remain |
| `EDEP-1001.5` | Google Cloud project with Calendar API enabled | Calendar connector availability | Open | Project owner configuration required |
| `EDEP-1001.6` | Google OAuth consent screen, client ID, client secret, authorized domain, and approved test or production users | Google account connection and token refresh | Open | Credentials and consent configuration required |
| `EDEP-1001.7` | User-approved Calendar event scope | Approval-gated study sessions, review dates, and reminders | Open | Live consent and external verification required |
| `EDEP-1001.8` | Google verification when required by selected scopes or user population | Production access beyond configured test users | Open | Subject to Google review requirements |
| `EDEP-1001.9` | Official Canvas or Moodle API access authorized by the user or institution | Future learning-management synchronization | Deferred | Resume after the shared Mentor runtime is fully verified |

## To do

- [ ] Configure the production OpenAI model and API key.
- [ ] Generate and securely store the connector encryption key.
- [ ] Deploy the API, worker, and persistent SQLite storage behind HTTPS.
- [ ] Enable Google Calendar API and configure the OAuth consent screen.
- [ ] Register the callback URL and add Google client credentials to the server.
- [ ] Connect a real test account and approve Calendar access.
- [ ] Verify one approved event, retry protection, token refresh, revocation, and restart recovery.
- [ ] Complete Google verification if required for public access.
- [ ] Add Canvas or Moodle only after official authorization is available.

## Completion gate

EDEP-1001 is complete when the deployed Mentor Agent can connect and revoke a real Google account, refresh expired access, recover from partial consent, create one exact approved Calendar event without duplication, and continue or recover durable work after the browser closes.

Secrets, OAuth tokens, provider content, and private learning records must never be stored in this document, source control, `VaultState`, or browser storage.

## Verification evidence

| Date | Work code | Evidence | Result |
|---|---|---|---|
| 2026-07-31 | `EDEP-1001.1` | Existing local key accepted by the Models API; `gpt-5.6-terra` completed a minimal Responses API request using 11 input and 5 output tokens | Local verification passed; production setup remains open |
