# Copy to .env.local and fill in your values:
#   cp .env.example .env.local

# Required for AI features (Career revise, assistant drawer)
OPENAI_API_KEY=your_key_here

# Server bind (defaults work for VPS + Docker)
API_HOST=0.0.0.0
API_PORT=8787
